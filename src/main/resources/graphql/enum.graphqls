enum Rarity {
	COMMON
	UNCOMMON
	RARE
	LEGENDARY
}

enum Type {
	CHIPS
	ADDITIVE_MULT
	MULTIPLICATIVE_MULT
	CHIPS_AND_ADDITIVE_MULT
	RETRIGGER
	ECONOMY
	EFFECT
}

enum Activation {
	PASSIVE
	INDEPENDENT
	ON_PLAYED
	ON_SCORED
	ON_HELD
	ON_DISCARD
	MIXED
	ON_OTHER_JOKERS
}